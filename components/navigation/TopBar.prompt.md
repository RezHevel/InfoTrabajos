Mobile screen header. The only place blur is used besides the web sticky header.

```jsx
<TopBar back onBack={goBack} title="Marta Quispe" subtitle="Electricista · 4,8"
  actions={<IconButton icon="heart" label="Guardar" />} />
```
