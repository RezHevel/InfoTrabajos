Switches views inside one screen (profile sections, admin queues).

```jsx
<Tabs value={tab} onChange={setTab}
  items={[{value:"perfil",label:"Perfil"},{value:"resenas",label:"Reseñas",count:126}]} />
```
