Confirmations and short flows. On mobile use `sheet`.

```jsx
<Dialog title="Enviar solicitud" description="Marta va a recibir tu pedido."
  onClose={close} footer={<><Button variant="ghost" onClick={close}>Cancelar</Button><Button>Enviar</Button></>} />
<Dialog sheet title="Filtros">…</Dialog>
```
