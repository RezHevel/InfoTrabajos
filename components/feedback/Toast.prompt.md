One line, one action. Never for errors that need a decision — use Dialog.

```jsx
<Toast tone="go">Solicitud enviada</Toast>
<Toast action={{ label: "Deshacer", onClick: undo }}>Reseña eliminada</Toast>
```
