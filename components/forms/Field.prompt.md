Wrap every input so labels, hints and errors stay consistent.

```jsx
<Field label="Zona de trabajo" hint="Podés elegir hasta 3 barrios" required>
  <Input icon="map-pin" placeholder="Ej. Villa Urquiza" />
</Field>
```
