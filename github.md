repo: RezHevel/InfoTrabajos
branch: main

## Last sync

date: 2026-09-16T16:05:00Z

### Updated in this project
- Repo still contains only `README.md` and `LICENSE`; the system is built from the project's own documents plus the supplied logo.
- Rebranded to the real logo palette: blue + navy + slate, with `--auth-*` tokens for the login surface.
- Added the mobile auth flow (login, 3-step registro with KYC), map view, cotizaciones and historial screens.
- Localized all copy to Mexican Spanish and Guadalajara context (MXN amounts, "tú" forms).

## Screen map

| Screen / artifact | Built from |
| --- | --- |
| `tokens/*.css` | product brief (no token source in repo) |
| `components/**` | product brief; Lucide (`lucide-icons/lucide`, `icons/*.svg`) |
| `ui_kits/app/*` | product brief — functional list (registro, perfiles, búsqueda, solicitudes, mensajería, calificaciones, geolocalización) |
| `ui_kits/web/*` | product brief — same list plus "Panel administrativo para gestión de usuarios y contenido" |
| `assets/icons/*.svg` | `lucide-icons/lucide@main`, `icons/` |
